# Deckseer Domain Context

Status: `redirected`

Shared project language now lives in `docs/CONTEXT.md`. This compatibility pointer remains so older handoffs that mention `docs/DOMAIN_CONTEXT.md` still lead to the canonical context document.
